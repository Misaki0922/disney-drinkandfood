# 9/22 東京ディズニーランド 当日プランナー

2026年9月22日（火）の当日プランを、タイムライン上で組み替えられるツール。

公開URL: https://misaki0922.github.io/disney-drinkandfood/planner.html

## ファイル構成

| ファイル | 役割 |
|---|---|
| `planner.html` | 本体（HTML/CSS/JS すべて内包・外部依存なし） |
| `planner.webmanifest` | PWA設定（ホーム画面追加用） |
| `planner-sw.js` | Service Worker（オフライン動作用） |
| `planner-icon-192.png` | ホーム画面アイコン 192px |
| `planner-icon-512.png` | ホーム画面アイコン 512px |
| `planner-icon-512-maskable.png` | Android用アイコン（外周が切られる想定で中央に寄せてある） |

「なに飲む？」（`index.html` / `sw.js`）とはファイル名がすべて異なるので、同じリポジトリに共存できる。
`planner-sw.js` は `planner` で始まるファイルだけを管轄し、「なに飲む？」のキャッシュには触らない。

## 更新時の必須作業

`planner.html` を修正したら、**必ず `planner-sw.js` の日付も変更する**。

```js
const CACHE = "planner-2026-09-02d";  // ← ここを変える
```

変えないと、ホーム画面から開いたときに古い内容が残る。

## 主な機能

### タイムライン（9:00〜21:00）
- 予定を実際の時刻・所要時間に応じたブロックで表示。時間が重なると横に並ぶ
- ブロックの色は7エリアの公式カラー（公式アプリのパークマップから抽出）
- 罫線は 1時間（実線・太）／30分（破線・時刻ラベル付き）／15分・45分（薄い破線）
- 現在時刻に赤い横線（30秒ごとに更新）
- ブロック左端の ⠿ を掴んで上下ドラッグで時刻変更（5分刻み・ドロップ時に時刻がふわっと表示）
- 🔒 ロック中のブロックはドラッグで動かない

### 予定の追加・編集
カテゴリ → エリア → 施設 → 時刻、の順にタップだけで選べる。
- アトラクション／買い物／カフェ・レストラン／フリー休憩・トイレ／その他（自由入力）
- アトラクションは「公式所要時間（固定）＋ 待ち時間（調整可）＝ 合計」を数式表示
- 時刻はロール式ピッカー（15分単位）。終了予定時刻が連動表示
- 🪑 着席カウント／🔒 移動ロックのスイッチ
- 前後の予定と重なると警告が出て、確定ボタンが無効になる（承知の上で追加も可）

### 終わったよチェック
ブロック右上の四角をタップすると、実際にかかった時間を入力するパネルが開く。
予定とズレていれば、そのあとの予定をまとめてずらせる（ロック中は据え置き）。

### おすすめ
タイムライン右端の 🔎 タブ。空き時間とその前後のエリアから、立ち寄れそうな施設を自動提案する。
- 施設名をタップ → そのまま予定追加画面へ
- ♡ → 「気になる」に保存

### 気になる（画面下部のドロワー）
保留中の候補。「✓ 採用」すると、収まる空き時間を探して自動配置する。

### 警告表示（⚠️）
- エリア間の移動時間が足りない場合
- レストランの営業時間外・閉店に間に合わない場合

### 共有
ヘッダー右上の 🔗 共有。プラン全体をURLに埋め込んで渡せる。
受け取った側は開くだけで同じプランが見える。

## データの出どころ

- **アトラクション一覧・所要時間**: 公式サイト各施設ページ（2026/8/31確認）
- **ショップ一覧・説明文・スタンバイパス対象**: 公式ショップ一覧（2026/8/31確認）
- **レストラン一覧・営業時間**: フリードリンク券対象24店＋センターストリート・コーヒーハウス
- **待ち時間**: 2025/10/12（E日）の実測データ。**9/22当日の混雑とは異なる目安**
- **エリア間の移動時間**: パーク配置からの見積もり。**公式数値ではない**

9/22時点の休止施設（スイスファミリー・ツリーハウス、白雪姫、空飛ぶダンボ、ベイマックス）と
終了済みのスペース・マウンテン、バズ・ライトイヤーは選択肢から除外済み。

## 保存について

編集内容はブラウザの localStorage に保存される（端末内のみ・サーバー送信なし）。
別の端末に渡したいときは 🔗 共有 を使う。
